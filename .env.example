# IndustrialSafetyAI — environment template
# Copy to `.env` and fill in. `.env` is gitignored and must never be committed.

# Optional: enables cloud-grade Gemini vision + grounded RAG synthesis.
# Leave blank to run fully offline (local CV + cached embeddings + deterministic engines).
GEMINI_API_KEY=
